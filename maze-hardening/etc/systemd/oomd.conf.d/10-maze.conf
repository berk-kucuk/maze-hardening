# Maze Linux — systemd-oomd thresholds. Pairs with zram so the userspace OOM
# killer acts on memory/swap pressure before the system grinds to a halt.
[OOM]
SwapUsedLimit=90%
DefaultMemoryPressureLimit=60%
DefaultMemoryPressureDurationSec=20s
